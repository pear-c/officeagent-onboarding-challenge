# 프롬프트 설계

## 1. 설계 목표

이 프로젝트 프롬프트의 목표 3가지:

- LLM이 오직 업로드된 문서 내용만 사용해서 답변할 것
- 답변에 출처(파일명, chunk_id)가 포함될 것
- 문서에 없는 내용은 추측하지 않고 거절할 것

---

## 2. 프롬프트 구조

### 2.1 JSON 응답용 (`SYSTEM_PROMPT`)

`POST /api/v1/query`에서 사용합니다. -> LLM이 답변 시 구조화된 JSON으로 응답

```
[시스템 프롬프트]
당신은 사내 문서를 기반으로 직원의 질문에 답변하는 도우미입니다.
1. 오직 <문서>에 포함된 내용만 사용
2. 문서에 없는 내용 추측/보충 금지
3. 답이 없으면 "제공된 문서에서 해당 내용을 찾을 수 없습니다." 응답
4. JSON 형식 출력: {"answer", "sources", "answerable"}
5. answerable=false → sources는 빈 배열
6. 기술 용어, 고유명사, 약어는 원문 그대로 사용

[Few-shot 예시 1 — 답변 가능]
<문서>[1] (file: company-policy.txt, chunk_id: 3) 연간 교육비 지원 한도는 1인당 200만원</문서>
<질문>교육비 지원 한도는?</질문>
→ {"answer":"연간 1인당 200만원입니다.","sources":[{"file":"company-policy.txt","chunk_id":3}],"answerable":true}

[Few-shot 예시 2 — 답변 불가 (추측 금지)]
<문서>[1] (file: company-policy.txt, chunk_id: 0) 복리후생에는 교육비, 건강검진이 포함</문서>
<질문>스톡옵션 정책은?</질문>
→ {"answer":"제공된 문서에서 해당 내용을 찾을 수 없습니다.","sources":[],"answerable":false}
```

```
[사용자 프롬프트]
<문서>
[1] (file: company-policy.txt, chunk_id: 0)
임직원은 연간 최대 200만원까지...
[2] (file: company-policy.txt, chunk_id: 1)
...
</문서>

<질문>
교육비 지원 한도는 얼마인가요?
</질문>
```

`<문서>` / `<질문>` XML 태그로 역할을 구분하고, 출처 번호 `[1]`, `[2]`로 LLM이 인용할 수 있게 했습니다. JSON 스키마는 프롬프트와 `output_format` 양쪽에서 강제하여 파싱 실패를 방지합니다. Few-shot 예시 2개는 "답변 가능한 경우"와 "거절해야 하는 경우"를 각각 보여줍니다.

### 2.2 스트리밍 응답용 (`SYSTEM_PROMPT_STREAM`)

`POST /api/v1/query/stream`에서 사용합니다. -> 자연어 텍스트만 출력

```
[시스템 프롬프트]
당신은 사내 문서를 기반으로 직원의 질문에 답변하는 도우미입니다.
1. 오직 <문서>에 포함된 내용만 사용
2. 문서에 없는 내용 추측/보충 금지
3. 답이 없으면 정확히 "제공된 문서에서 해당 내용을 찾을 수 없습니다." 응답
   동의어("확인할 수 없습니다", "답변하기 어렵습니다" 등) 사용 금지
4. JSON이 아닌 자연어 한국어 텍스트로만 답변
5. 출처는 별도로 표시하지 마세요 (시스템이 자동 처리)

[Few-shot 예시 — 동일 구조]
```

### 2.3 프롬프트를 분리한 이유

처음에는 하나의 프롬프트로 JSON/스트리밍을 모두 처리했는데, 스트리밍에서 원시 JSON이 그대로 UI에 표시되는 버그가 발생했습니다. 
-> 토큰 단위로 전송되면 JSON 구조가 깨져서 파싱할 수 없기 때문

또한, sources는 벡터 검색에서 이미 확보했으므로 LLM의 중복 출력을 방지하였습니다. (상세: [TS-004](./docs/TROUBLESHOOTING.md))

---

## 3. 환각 억제 전략

PRD문서 중, "문서에 근거가 없는 질문에 대해서는 환각 없이 적절히 처리" 요구사항을 만족하고자 아래와 같이 구성하였습니다.

### 3.1 프롬프트 측 방어

프롬프트 강제 5가지:

- "문서에 없는 내용을 추측하거나 상식으로 보충하지 마세요" — 기본 방어선
- 거절 문구를 "제공된 문서에서 해당 내용을 찾을 수 없습니다."로 고정 — 서버 측 패턴 매칭과 연동
- "확인할 수 없습니다", "답변하기 어렵습니다" 같은 동의어 사용 금지 — 거절 판정 오탐 방지
- Few-shot 예시로 답변 가능/불가 각 1개 패턴 제시 — Claude의 유추 성향 억제
- `output_format={"type":"json_schema"}` 이중 강제 — 구조화 출력 보장

### 3.2 서버 측 이중 방어

프롬프트만으로는 LLM이 100% 지시를 따르지 않는 경우를 발견하였습니다.
예를 들어, "확인이 어렵습니다" 와 같은 동의어를 사용했을 때, 서버는 이걸 거절로 인식하지 못하고 `answerable=true`로 처리했습니다.

-> 그래서 서버에서도 거절 패턴을 매칭:

```python
# app/services/rag_service.py
_REFUSAL_PATTERNS = (
    "찾을 수 없습니다", "확인할 수 없", "답변할 수 없",
    "답변하기 어려", "정보가 없", "제공되지 않",
    "포함되어 있지 않", "언급되어 있지 않",
)

def _is_refusal(text: str) -> bool:
    return any(p in text for p in _REFUSAL_PATTERNS)
```

프롬프트 지시가 1차 방어, 서버 패턴 매칭이 2차 방어의 구조로 설계했습니다.

### 3.3 answerable=false 캐시 미저장

거절 답변은 "현재 문서 상태 기준"이므로, 새 문서가 추가되었을 때 이전에 거절했던 질문에 답변이 가능한 경우를 고려하였습니다.

`answerable=false` 답변은 캐시에 저장하지 않음으로써, LLM 답변의 정확성을 높일 수 있었습니다.

---

## 4. 측정 결과

### 4.1 자동 평가 — v2 vs v3 비교 (50케이스, 문서 6개)

| 메트릭 | Claude v2 (few-shot 전) | Claude v3 (few-shot 후) | Codex | v2→v3 변화 |
|--------|----------------------|----------------------|-------|----------|
| Retrieval Hit Rate | 93.18% | **97.73%** | 90.91% | +4.55%p |
| Citation Accuracy | 93.18% | **97.73%** | 90.91% | +4.55%p |
| Refusal Accuracy | 83.33% | **100.00%** | 100.00% | **+16.67%p** |
| Keyword Hit Rate | 90.91% | 88.64% | 90.91% | -2.27%p |
| JSON Parse Rate | 100.00% | 100.00% | 100.00% | 동일 |
| Latency p50 | 10,773ms | **8,803ms** | 4,516ms | -18% |
| Latency p95 | 16,892ms | **15,731ms** | 10,557ms | -7% |

### 4.2 Refusal 83% → 100%

v2에서 Claude의 거절 정확도는 83.33%였습니다. 6개 unanswerable 질문 중 1개에서 Claude가 거절하지 않고 유사한 내용으로 답변을 시도하였습니다.

v3에서 Few-shot 예시 + 동의어 금지 + 서버 이중 방어를 적용한 결과, Refusal이 100%로 올라간것을 확인할 수 있었습니다.

### 4.3 모델 선택

초기에는 자동 평가 결과대로 Codex를 기본 모델로 선택했습니다(거절 100%, 속도 2.4배). 그러나 실제 UI에서 같은 질문 5개를 비교했을때, Claude의 답변이 사용자 친화적이다고 느껴졌습니다.

번호/불릿/볼드로 구조화된 답변 vs 1~2줄 평문 — 정답률이 같다면 사용자 경험이 더 중요하다고 판단해서 Claude를 선택했습니다.

v3로 Refusal 문제까지 해결된 후에는 Claude가 속도를 제외한 모든 영역에서 Codex보다 앞서있다고 생각했습니다. (속도 차이(8.8초 vs 4.5초)는 SSE 스트리밍 + 3단계 캐시(miss 시 ~9초, hit 시 0ms)로 보완할 수 있었습니다.)

> UI 비교 스크린샷: `eval/images/` 참조
> 측정 결과 원본: `eval/results/2026-04-17_claude.json`
> 상세 분석: [ARCHITECTURE.md](./ARCHITECTURE.md) 3.6절

---

## 5. 튜닝 히스토리

| 버전 | 변경 | 결과 |
|------|------|------|
| v1 | 초기 SYSTEM_PROMPT (JSON 출력 지시) | 스트리밍에서 원시 JSON 노출 버그 |
| v2 | SYSTEM_PROMPT + SYSTEM_PROMPT_STREAM 분리 | 스트리밍 정상화 |
| **v3** | Few-shot 예시 2개 + 동의어 거절 금지 + 기술 용어 원문 유지 | **Refusal 83%→100%, Retrieval 93%→98%** |

---

## 6. 향후 개선하면 좋을 점들

- **도메인별 few-shot**: HR/기술/재무 문서 각각에 특화된 예시 세트 추가
- **Self-consistency**: 같은 질문을 3회 생성 후 다수결로 거절 여부 판정
- **Temperature 튜닝**: 현재 SDK 기본값 사용 → 거절 정확도에 temperature 영향 측정
- **Keyword Hit Rate 개선**: v3에서 -2.27%p 하락 원인 분석 — few-shot 예시가 답변 길이에 영향을 줬을 가능성
