# 프롬프트 설계

## 1. 설계 목표

| 목표 | 설명 |
|------|------|
| **문서 근거 기반 답변** | LLM이 오직 업로드된 문서 내용만 사용하여 답변 |
| **출처 추적 가능한 구조화 출력** | 답변에 파일명 + chunk_id가 포함된 JSON/자연어 출력 |
| **답변 불가 시 명확한 거절** | 문서에 없는 내용을 추측하지 않고, 정해진 문구로 거절 |
| **환각(Hallucination) 억제** | Few-shot 예시 + 동의어 금지 + 서버 측 이중 방어 |

---

## 2. 프롬프트 구조

### 2.1 JSON 응답용 (`SYSTEM_PROMPT`)

`POST /api/v1/query` 엔드포인트에서 사용. LLM이 구조화된 JSON으로 응답.

```
[시스템 프롬프트]
당신은 사내 문서를 기반으로 직원의 질문에 답변하는 도우미입니다.
1. 오직 <문서>에 포함된 내용만 사용
2. 문서에 없는 내용 추측/보충 금지
3. 답이 없으면 "제공된 문서에서 해당 내용을 찾을 수 없습니다." 응답
4. JSON 형식 출력: {"answer", "sources", "answerable"}
5. answerable=false → sources는 빈 배열
6. 기술 용어, 고유명사, 약어는 원문 그대로 사용

[Few-shot 예시 1 — 답변 가능]
<문서>[1] (file: company-policy.txt, chunk_id: 3) 연간 교육비 지원 한도는 1인당 200만원</문서>
<질문>교육비 지원 한도는?</질문>
→ {"answer":"연간 1인당 200만원입니다.","sources":[{"file":"company-policy.txt","chunk_id":3}],"answerable":true}

[Few-shot 예시 2 — 답변 불가 (추측 금지)]
<문서>[1] (file: company-policy.txt, chunk_id: 0) 복리후생에는 교육비, 건강검진이 포함</문서>
<질문>스톡옵션 정책은?</질문>
→ {"answer":"제공된 문서에서 해당 내용을 찾을 수 없습니다.","sources":[],"answerable":false}
```

```
[사용자 프롬프트]
<문서>
[1] (file: company-policy.txt, chunk_id: 0)
임직원은 연간 최대 200만원까지...
[2] (file: company-policy.txt, chunk_id: 1)
...
</문서>

<질문>
교육비 지원 한도는 얼마인가요?
</질문>
```

**설계 의도**:
- `<문서>` / `<질문>` XML 태그로 역할을 명확히 구분
- 출처 번호 `[1]`, `[2]`로 LLM이 인용할 수 있도록 유도
- JSON 스키마를 프롬프트 + `output_format` 이중으로 강제하여 파싱 실패 방지
- Few-shot 예시 2개(answerable/unanswerable)로 거절 패턴 사전 학습

### 2.2 스트리밍 응답용 (`SYSTEM_PROMPT_STREAM`)

`POST /api/v1/query/stream` SSE 엔드포인트에서 사용. 자연어 텍스트만 출력.

```
[시스템 프롬프트]
당신은 사내 문서를 기반으로 직원의 질문에 답변하는 도우미입니다.
1. 오직 <문서>에 포함된 내용만 사용
2. 문서에 없는 내용 추측/보충 금지
3. 답이 없으면 정확히 "제공된 문서에서 해당 내용을 찾을 수 없습니다." 응답
   동의어("확인할 수 없습니다", "답변하기 어렵습니다" 등) 사용 금지
4. JSON이 아닌 자연어 한국어 텍스트로만 답변
5. 출처는 별도로 표시하지 마세요 (시스템이 자동 처리)

[Few-shot 예시 — 동일 구조]
```

### 2.3 프롬프트를 분리한 이유

JSON 엔드포인트와 SSE 스트리밍 엔드포인트에서 **동일 프롬프트를 사용하면 안 됨**:

| 문제 | 원인 | 해결 |
|------|------|------|
| 스트리밍에서 원시 JSON이 UI에 노출 | 토큰 단위 전송 시 JSON 구조가 깨져서 파싱 불가 | 스트리밍 전용 프롬프트 분리 (자연어만 출력) |
| sources 중복 전송 | JSON 프롬프트가 sources를 생성하지만, 벡터 검색에서 이미 확보 | 스트리밍 프롬프트에서 "출처 표시 금지" 명시 |

> 이 분리는 실제 버그(TS-004)에서 발견. 상세: [TROUBLESHOOTING.md](./docs/TROUBLESHOOTING.md)

---

## 3. 환각 억제 전략

PRD 요구사항: "문서에 근거가 없는 질문에 대해서는 환각 없이 적절히 처리해야 한다."

### 3.1 프롬프트 측 방어

| 전략 | 프롬프트 규칙 | 효과 |
|------|------------|------|
| **명시적 금지** | "문서에 없는 내용을 추측하거나 상식으로 보충하지 마세요" | 기본 방어선 |
| **거절 문구 고정** | "정확히 '제공된 문서에서 해당 내용을 찾을 수 없습니다.'로 답하세요" | 서버 측 패턴 매칭과 연동 |
| **동의어 금지** | "확인할 수 없습니다, 답변하기 어렵습니다 등 사용 금지" | 거절 판정 오탐 방지 |
| **Few-shot 예시** | 답변 가능/불가 각 1개 패턴 제시 | Claude의 "도움이 되려는" 성향 억제 |
| **출력 스키마 강제** | `output_format={"type":"json_schema"}` 이중 강제 | 구조화 출력 보장 |

### 3.2 서버 측 이중 방어

프롬프트만으로는 LLM이 100% 지시를 따르지 않을 수 있음. 서버 측에서 추가 검증 수행:

```python
# app/services/rag_service.py
_REFUSAL_PATTERNS = (
    "찾을 수 없습니다", "확인할 수 없", "답변할 수 없",
    "답변하기 어려", "정보가 없", "제공되지 않",
    "포함되어 있지 않", "언급되어 있지 않",
)

def _is_refusal(text: str) -> bool:
    return any(p in text for p in _REFUSAL_PATTERNS)
```

**이중 방어 효과**: 프롬프트가 동의어를 쓰더라도 서버가 잡아냄. 반대로 서버 패턴에 없는 새 표현이 나와도 프롬프트 지시가 1차 방어.

### 3.3 answerable=false 캐시 미저장

거절 답변은 **현재 문서 상태 기준**이므로, 새 문서가 추가되면 이전에 거절했던 질문에 답변할 수 있어야 한다. `answerable=false` 답변은 캐시에 저장하지 않음.

---

## 4. 측정 결과

### 4.1 자동 평가 — v2 vs v3 비교 (50케이스, 문서 6개)

| 메트릭 | Claude v2 (few-shot 전) | Claude v3 (few-shot 후) | Codex | v2→v3 변화 |
|--------|----------------------|----------------------|-------|----------|
| Retrieval Hit Rate | 93.18% | **97.73%** | 90.91% | **+4.55%p** |
| Citation Accuracy | 93.18% | **97.73%** | 90.91% | **+4.55%p** |
| **Refusal Accuracy** | 83.33% | **100.00%** | 100.00% | **+16.67%p** |
| Keyword Hit Rate | 90.91% | 88.64% | 90.91% | -2.27%p |
| JSON Parse Rate | 100.00% | 100.00% | 100.00% | 동일 |
| Latency p50 | 10,773ms | **8,803ms** | 4,516ms | **-18%** |
| Latency p95 | 16,892ms | **15,731ms** | 10,557ms | -7% |

### 4.2 v3 핵심 성과 — Refusal 83% → 100%

v2에서 Claude의 Refusal 83.33%는 이 프로젝트의 **가장 중요한 발견**이었음:

1. **모델 훈련 철학의 차이**: Claude는 "도움이 되려는(helpful)" 성향이 강하여 문서에 비슷한 내용이 있으면 유추 시도
2. **과잉 추론 (Over-inference)**: "스톡옵션 정책은?" 질문에 급여/복리후생 문서가 context로 주어지면, "스톡옵션은 언급되지 않지만, 급여 구성은..."처럼 관련 내용으로 답변 시도

**v3에서 Few-shot 예시 + 동의어 금지 + 서버 이중 방어를 적용한 결과, Refusal이 100%로 개선.** Claude가 Codex와 동일한 거절 정확도를 달성하면서, 답변 품질(구조화, 가독성)은 유지.

### 4.3 최종 모델 선택

v3 적용 후 Claude가 **모든 메트릭에서 Codex 이상**:

| 관점 | Claude v3 | Codex | 판정 |
|------|----------|-------|------|
| Retrieval Hit Rate | **97.73%** | 90.91% | Claude |
| Refusal Accuracy | **100%** | 100% | 동률 |
| 답변 구조 | 번호/불릿/볼드 마크다운 구조화 | 1~2줄 평문 | Claude |
| 정보량 | 부가 설명 + 관련 맥락까지 제공 | 핵심 키워드만 | Claude |
| 투명성 | 답변의 한계를 솔직히 고지 | 없음 | Claude |
| 속도 | 8,803ms | **4,516ms** | Codex |

**결론**: Refusal 약점이 해소된 후, Claude가 속도를 제외한 모든 영역에서 우위. 속도는 SSE 스트리밍 + 3단계 캐시로 사용자 체감을 보완. Claude를 기본 모델로 확정.

> UI 비교 스크린샷: `eval/images/` 참조
> 측정 결과 원본: `eval/results/2026-04-17_claude.json`
> 상세 분석: [ARCHITECTURE.md](./ARCHITECTURE.md) 3.6절, [TROUBLESHOOTING.md](./docs/TROUBLESHOOTING.md) INT-008, INT-008-1

---

## 5. 튜닝 히스토리

| 버전 | 변경 | 결과 |
|------|------|------|
| v1 | 초기 SYSTEM_PROMPT (JSON 출력 지시) | JSON/스트리밍 공용 → 스트리밍에서 원시 JSON 노출 버그 |
| v2 | SYSTEM_PROMPT + SYSTEM_PROMPT_STREAM 분리 | 스트리밍 정상화 |
| **v3** | Few-shot 예시 2개 + 동의어 거절 금지 + 기술 용어 원문 유지 | **Refusal 83%→100%, Retrieval 93%→98%** |

---

## 6. 향후 개선 방향

- **도메인별 few-shot**: HR/기술/재무 문서 각각에 특화된 예시 세트
- **Self-consistency**: 같은 질문을 3회 생성 후 다수결로 거절 여부 판정
- **Temperature 튜닝**: 현재 SDK 기본값 사용 → 거절 정확도에 temperature 영향 측정
- **Keyword Hit Rate 개선**: v3에서 -2.27%p 하락 원인 분석 (few-shot 예시가 답변 길이에 영향?)
