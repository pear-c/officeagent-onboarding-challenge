# Retrobot 회고 — 2026-04-15 11:30 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 5 |
| 총 프롬프트 | 5 |
| 에러 발생 | 5 (대부분 cp949 인코딩 에러) |

## 세션별 활동

### 세션 `2f7c86f5` (11:21:56 ~ 11:21:59 KST)

**프롬프트:**
1. Retrobot SKILL.md 직접 실행 요청 (프롬프트가 지시문 없이 본문만 전달되어 실질 작업 없이 종료)

**발생한 에러:** 없음 (실행 자체가 불발)

### 세션 `aa5f8289` (11:23:37 ~ 11:24:50 KST)

**프롬프트:**
1. "아래 SKILL.md 지침에 따라 지금 즉시 KPT 회고를 생성하고 retros/ 디렉토리에 파일로 저장하세요."

**사용 도구:** Bash ×8, Write ×2

**발생한 에러:**
- Python 스크립트 내 `UnicodeEncodeError: 'cp949' codec can't encode character '\u2014'` — 한글 Windows 콘솔 기본 인코딩 문제

### 세션 `995cc370` (11:26:34 ~ 11:26:38 KST)

Retrobot 스킬 본문만 전달된 불발 세션. 도구 호출 없음.

### 세션 `ff5402e5` (11:26:42 ~ 11:28:24 KST)

**사용 도구:** Bash ×9, Read ×1, Write ×1, Edit ×1

**발생한 에러:**
- 동일한 cp949 UnicodeEncodeError 재발
- 이후 `PYTHONIOENCODING=utf-8` 설정 혹은 `sys.stdout.reconfigure(encoding='utf-8')`로 우회 추정

### 세션 `45dd15ec` (11:30:11 ~ 11:30:40 KST, 진행 중)

**사용 도구:** Bash ×6 (현재 회고 생성 중)

**발생한 에러:** 초기 분석 스크립트에서 동일한 cp949 에러 → utf-8 강제 후 복구

## KPT 회고

### Keep (좋았던 것)
- **로그 수집 파이프라인이 안정적으로 동작**: `~/.claude/projects/` 경로 매핑 규칙(`/` → `-`)을 일관되게 사용하여 여러 세션에서 반복 성공.
- **병렬 Bash 호출 활용**: 디렉토리 탐색·시각 획득 등 독립 작업을 한 메시지에서 병렬 실행하여 오버헤드 감소.
- **짧은 세션의 명확한 목적 분리**: Retrobot 작업 세션과 불발 세션이 혼재되어도 타임스탬프 기반으로 쉽게 구분 가능.

### Problem (나빴던 것)
- **Windows cp949 인코딩 에러가 세 세션 연속 재발**: Python 스크립트에서 유니코드 대시(`—`) 출력 시 `UnicodeEncodeError`가 반복됨. 매번 같은 실수를 반복했다.
- **"불발 세션" 2건**: SKILL.md 본문이 지시문 없이 그대로 입력되어 에이전트가 단순 복사 출력만 한 사례. 사용자 의도 해석 실패.
- **스크립트 시행착오**: 세션당 Bash 8~9회 호출 중 상당수는 인코딩/디버깅 재시도 — 한 번에 안정적으로 작성했다면 3~4회로 충분했을 것.

### Try (개선해야 할 것)
- **Python 스크립트 실행 시 반드시 `PYTHONIOENCODING=utf-8` 프리픽스 또는 `sys.stdout.reconfigure(encoding='utf-8')` 초기 호출을 기본 템플릿으로 사용** — Windows 환경 전제.
- **Retrobot 트리거 프롬프트를 명확한 지시문으로 래핑**: SKILL.md를 그대로 전달하지 말고 "아래 스킬에 따라 실행하라"는 래퍼 문장을 항상 앞에 붙인다 (이미 최근 세션에서는 적용 중).
- **회고 생성 스크립트를 `retrobot/` 하위에 고정 파일로 저장**: 매번 인라인 Python을 새로 작성하는 대신 재사용 가능한 `retrobot/analyze.py`를 두어 인코딩·파싱 로직 중복을 제거한다.

<!-- 직접 추가하세요 -->

## 타임라인

| 시각 (KST) | 세션 | 주요 활동 |
|------|------|----------|
| 11:21 | 2f7c86f5 | 불발 (SKILL 본문만 입력) |
| 11:23 | aa5f8289 | Retrobot 실행, cp949 에러 후 우회 |
| 11:26 | 995cc370 | 불발 |
| 11:26 | ff5402e5 | Retrobot 실행, 인코딩 에러 재발·복구 |
| 11:30 | 45dd15ec | 현재 회고 생성 세션 |
