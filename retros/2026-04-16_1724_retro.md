# Retrobot 회고 — 2026-04-16 17:24 KST

> 자동 생성된 회고입니다. 직접 편집하여 보완하세요.

## 요약

| 항목 | 값 |
|------|-----|
| 세션 수 | 1 |
| 총 프롬프트 | 1 |
| 에러 발생 | 0 |
| 분석 기간 | 2026-04-16 11:57 ~ 17:24 (직전 회고 이후) |
| 신규 커밋 | 1 (`16e0319`) |

## 세션별 활동

### 세션 `9661a7c0` (2026-04-16 17:23 ~ 17:24)

**프롬프트:**
1. SKILL.md 지침에 따라 KPT 회고 생성 요청 (Retrobot 자동 실행)

**사용 도구:** Bash, Read, Write — 로그 탐색 및 회고 파일 생성 전용

**발생한 에러:** 없음

**주요 활동:**
- `~/.claude/projects/` 하위에서 현재 프로젝트 로그 디렉토리 식별
- 직전 회고(`2026-04-16_2100_retro.md`) 범위 확인하여 중복 방지
- 직전 회고 이후 신규 커밋(`16e0319` — 거절 판정 강화 + few-shot + 캐시 메타데이터 저장) 확인
- 현 세션 로그 파싱 후 회고 문서 작성

---

## 직전 회고 이후 프로젝트 진전

### 신규 커밋: `16e0319` feat: 거절 판정 강화 + few-shot + 캐시 메타데이터 저장

RAG 파이프라인 품질 개선에 초점을 맞춘 커밋. 세 가지 축:

1. **거절 판정 강화** — 근거 없는 질문에 대한 환각 억제 로직 보강. 평가 비중 15%(프롬프트 엔지니어링)의 핵심 항목.
2. **Few-shot 프롬프팅 도입** — 답변 포맷/톤 일관성 확보.
3. **캐시 메타데이터 저장** — 캐시 히트/무효화 판정에 필요한 메타(section/text)를 캐시에 포함. 20%(캐싱 전략) 항목과 직결.

직전 2100 회고에서 지적된 "RAG 파이프라인 개발은 순조롭다"는 흐름이 계속 유지되고 있음.

## KPT 회고

### Keep (좋았던 것)

- **에러 제로 회고 생성**: 이번 세션은 에러 없이 완료. 직전 8개 세션 누적 67건 에러와 극명히 대비된다. 원인은 (1) Windows에서 `python3` 대신 `python` 직접 사용, (2) Bash + Python 인라인 스크립트로 파싱 표준화, (3) 직전 회고를 먼저 읽고 범위를 좁힌 것. 이전 Try 항목("Windows Python 호환성 고정", "JSONL 파싱 표준화")이 실전에서 작동했다.
- **증분 회고 원칙 준수**: 직전 회고가 커버한 세션을 재분석하지 않고, 신규 세션과 신규 커밋만 범위에 포함했다. 타임라인이 중복 없이 누적된다.
- **RAG 품질 축에 집중한 커밋**: 단일 커밋에 거절 판정/few-shot/캐시 메타데이터를 묶어 처리. 평가 비중이 높은 프롬프트·캐싱 영역을 동시에 공략한 응집도 높은 변경.
<!-- 직접 추가하세요 -->

### Problem (나빴던 것)

- **단일 세션·단일 프롬프트로 회고 내용이 얇다**: 이번 회고 기간(약 5시간 반) 동안 실제 개발 세션이 없고, Retrobot 실행 세션 하나만 존재. 회고 자체는 성공했으나 "성장의 흐름을 타임라인으로 기록"한다는 목적에서 보면 정보 밀도가 낮다.
- **회고 실행 주기가 과도하게 잦을 가능성**: 직전 회고(2100)와 본 회고(1724) 사이 간격에 비해 실제 변경은 커밋 1건뿐. post-commit 훅이 매 커밋마다 회고를 생성한다면, 회고 파일 수 대비 학습 가치가 희석된다.
<!-- 직접 추가하세요 -->

### Try (개선해야 할 것)

- **회고 생성 최소 조건 추가**: 직전 회고 이후 (1) 신규 커밋 수 ≥ N, 또는 (2) 신규 세션 수 ≥ N 중 하나를 충족할 때만 회고를 생성하도록 `retrobot/SKILL.md`에 기준 명시. 조건 미달 시 "기존 회고에 추가 기록" 또는 스킵.
- **신규 커밋 diff 요약을 회고에 자동 포함**: 이번에 수동으로 정리한 "신규 커밋 분석" 섹션을 SKILL.md 3단계 템플릿에 정식 포함하여, 코드 변경의 의도가 회고에 반영되도록 한다.
- **세션 간 학습 메모리 축적**: 본 세션이 에러 없이 끝난 이유(python 명령 선택, 인라인 스크립트 사용)를 `memory/` 영역에 feedback 메모리로 저장하여 다음 Retrobot 세션도 동일 경로를 타게 한다.
<!-- 직접 추가하세요 -->

## 타임라인

| 시각 | 세션 | 주요 활동 |
|------|------|----------|
| 04-16 12:xx | (커밋) | `16e0319` feat: 거절 판정 강화 + few-shot + 캐시 메타데이터 저장 |
| 04-16 17:23 | `9661a7c0` | Retrobot 회고 생성 (에러 0건, 증분 분석) |

---

> **핵심 인사이트**: 직전 회고의 Try 항목(Windows Python 호환성, JSONL 파싱 표준화)이 실제 성과로 연결되어 이번 세션은 에러 제로로 종료. 다음 과제는 회고의 "밀도"를 높이는 방향 — 커밋 diff 자동 요약과 회고 트리거 최소 조건 설정이 다음 라운드의 Try다.
