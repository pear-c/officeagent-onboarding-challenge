"""평가 메트릭 계산."""

# TODO: 04-eval 단계에서 구현
# - retrieval_hit_rate(results, golden) -> float
# - citation_accuracy(results, golden) -> float
# - refusal_accuracy(results, golden) -> float
# - json_parse_rate(results) -> float
# - latency_percentile(results, p) -> float
