"""임베딩 래퍼 — sentence-transformers + bge-m3."""


class Embedder:
    """임베딩 모델 로딩 및 텍스트 → 벡터 변환."""

    # TODO: 02-ingestion 단계에서 구현
    # - __init__(model_name, device) — 모델 로딩 (앱 시작 시 1회)
    # - embed(texts: list[str]) → list[list[float]]
    # - embed_query(query: str) → list[float]
    pass
