"""Ingestion 비즈니스 로직 — 텍스트 추출 → 청킹 → 임베딩 → 저장."""


class IngestService:
    """문서 수집 파이프라인 조율."""

    # TODO: 02-ingestion 단계에서 구현
    # - extract_text(file) → str
    # - chunk(text, filename) → list[Chunk]
    # - embed_and_store(chunks) → None
    # - save_document_hash(filename, content) → None
    pass
