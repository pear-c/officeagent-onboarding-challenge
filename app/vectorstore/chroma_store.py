"""Chroma 벡터 DB 클라이언트."""


class ChromaStore:
    """Chroma 서버 연결 + 벡터 CRUD."""

    # TODO: 02-ingestion 단계에서 구현
    # - __init__(host, port, collection_name) — 클라이언트 연결
    # - add(ids, embeddings, documents, metadatas) → None
    # - search(query_embedding, top_k) → list[SearchResult]
    # - delete_by_document(filename) → None
    # - count() → int
    pass
